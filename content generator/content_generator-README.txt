Usage for content_generator.py:

$ python3 content_generator.py [filename]

filename: (optional) specify input csv file

This program requires the tkinter and wikipedia packages.

Note that the input file must have a header line at the top.
Example where the keywords are puppy and dog:

input_keywords
puppy;dog